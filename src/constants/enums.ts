export enum Directions {
  RTL = "rtl",
  LTR = "ltr",
}

export enum Languages {
  ENGLISH = "en",
  ARABIC = "ar",
}

export enum Pages {
  HOME = "",
  LOGIN = "login",
  TRS = "TRS",
  SURVEY="survey"
}

export enum Orders {
  ASC = "asc",
  DSC = "desc",
}

export enum FormActions {
  ADD = "Add",
  EDIT = "Edit",
}

export enum Navigate {
  NEXT = "next",
  PREV = "prev",
}
